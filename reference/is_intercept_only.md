# check if the formula is intercept only

outputs logical values for a formula; to identify intercept-only
formula.

## Usage

``` r
is_intercept_only(form)
```

## Arguments

- form:

  Regression formula

## Value

`TRUE` for intercept-only; `FALSE` otherwise
