# Get unique month from Date

`unique_month` converts observed dates into unique months to help
visualize sampled months

## Usage

``` r
unique_month(Rdate)
```

## Arguments

- Rdate:

  standard date format in R

## Value

a vector of characters with `month-year`, e.g., `4-2012`.
